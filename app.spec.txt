# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

added_files = [
    ('app/resources/*', 'app/resources'),
    ('data', 'data'),
]

a = Analysis(
    ['app/run_app.py'],
    pathex=[],
    binaries=[],
    datas=added_files,
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_files, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='Sistem_Deteksi_Mengantuk',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # Set True untuk debugging, False untuk production
    icon='app/resources/logo_pens.ico',  # Optional: tambahkan file .ico
)